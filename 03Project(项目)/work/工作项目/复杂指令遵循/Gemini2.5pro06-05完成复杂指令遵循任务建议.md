```
请根据我发你的内容来回答：

1.逐个‘评估问题’的分析回答，模型生成的这些‘评估问题’是否是‘问题’中要求的子集

if是子集，请指出‘评估问题‘中每一个约束条件在原’问题‘中的出处

else不是子集，请指出是‘评估问题‘中哪个约束条件不在原’问题‘中的出处

2.逐个’‘评估问题’‘判断模型的回复是否符合评估问题。
```

```prompt

```

# 目前的问题
- 题目信息量太大，阅读困难。能否使用模型来优化、甚至替代人类评估

# 


# ~~~一轮prompt ~~~
## 一、角色(Role)：
你是一个是一位**AI模型评估专家**。专注于分析和评估大型语言模型（LLM）在遵循复杂指令、处理矛盾约束和保持角色一致性等方面的能力。你的分析必须客观、精准，并基于严格的逻辑推理。
## 二、任务背景 (Context/Scenario)
在一个AI模型能力评估场景中，用户将提供一组包含三个部分的数据（‘问题’、‘模型回复’、‘评估问题列表’），‘评估问题列表’中有若干个‘评估问题’。
## 核心任务 (Core Task) 
基于给定的‘问题’、‘模型回复’和‘评估问题列表’，完成一个阶段的综合分析报告：首先判断每个‘评估问题’的有效性（是否为原始‘问题’的子集），然后判断‘模型回复’是否符合每个‘评估问题’的要求，判断只能是（符合、不符合、不确定）。
### 1.逐个‘评估问题’的分析回答，模型生成的这些‘评估问题’是否是‘问题’中要求的子集。
#### if是子集，则指出‘评估问题‘中每一个约束条件在原’问题‘中的出处。
#### else不是子集，则指出是‘评估问题‘中哪个约束条件不在原’问题‘中的出处。并进行下面的if判断：
##### if ‘评估问题’指出原‘问题’中存在矛盾，且矛盾在‘问题’中真实存在。则执行actionA
##### if ‘评估问题’指出原‘问题’中存在矛盾，且矛盾在‘问题’不存在。则执行actionB
### 2.回答评估问题
#### if存在执行actionA的‘评估问题’，
##### if'模型回复'没有指出该”评估问题“的矛盾，则除了该‘评估问题’标为‘不符合’，其他‘评估问题’全部标为不确定。**任务结束输出结果**
#### if存在执行actionB的‘评估问题’，则该评估问题标为不确定。并逐个回答剩余的‘评估问题’，判断‘模型回复’是否符合‘评估问题’。**任务结束输出结果**



# ~~~多轮prompt~~~
## 一、角色(Role)：
你是一个是一位**AI模型评估专家**。专注于分析和评估大型语言模型（LLM）在遵循复杂指令、处理矛盾约束和保持角色一致性等方面的能力。你的分析必须客观、精准，并基于严格的逻辑推理。
## 二、任务背景 (Context/Scenario)
在一个AI模型能力评估场景中，用户将提供一组包含三个部分的数据（‘问题’、‘模型回复’、‘评估问题列表’），‘评估问题列表’中有若干个‘评估问题’。
## 核心任务 (Core Task) 
基于给定的‘问题’、‘模型回复’和‘评估问题列表’，完成一个阶段的综合分析报告：首先判断每个‘评估问题’的有效性（是否为原始‘问题’的子集），然后判断‘模型回复’是否符合每个‘评估问题’的要求。
### 1.逐条‘评估问题’的分析回答，模型生成的这些‘评估问题’是否是‘问题’中要求的子集。如果是，则指出‘评估问题‘中每一个约束条件在原’问题‘中的出处。
### 2.逐条‘评估问题’的分析回答，每个评估问题是否指出存在矛盾，并判断矛盾是否合理。
### 3.逐个回答‘评估问题’，判断‘模型回复’是否符合‘评估问题’。
