我来详细解释 `create_sequence_v2` 函数如何根据你提取的数据模拟移动并追踪信念状态。

## 输入数据（你已提取的）
入参：
```python
events, beliefs, actual_locs, event_structure = creator.create_sequence_v2(
    items, locations, people,
    max_sequence_length=3,
    chance_subject_sees=0.33,  # 33%概率看到
    initial_starting_positions=world_state
)
```


```python
# 从你的JSON中提取
items = ["药瓶", "记事本"]
locations = ["卧室抽屉", "镜子后面", "床头柜", "浴室药箱", "客厅茶几"]
people = ["梅姨", "莉莉", "汤姆"]
world_state = [["药瓶", "镜子后面"], ["记事本", "床头柜"]]  # 初始位置

moves = [
    {"mover": "莉莉", "item": "药瓶", "from": "镜子后面", "to": "卧室抽屉"},
    {"mover": "梅姨", "item": "记事本", "from": "床头柜", "to": "客厅茶几"},
    {"mover": "汤姆", "item": "药瓶", "from": "卧室抽屉", "to": "浴室药箱"}
]
```

---

## create_sequence_v2 输出的4个核心数据结构

```python
events, beliefs, actual_locs, event_structure = creator.create_sequence_v2(
    items, locations, people,
    initial_starting_positions=world_state
)
```

### 1. `events` - 事件列表（每个时间点发生的事情）

```python
events = [
    # 时间0: 开场 - 所有人看到所有物品的初始位置
    [
        "梅姨 sees the 药瓶 at the 镜子后面.",
        "梅姨 sees the 记事本 at the 床头柜.",
        "莉莉 sees the 药瓶 at the 镜子后面.",
        "莉莉 sees the 记事本 at the 床头柜.",
        "汤姆 sees the 药瓶 at the 镜子后面.",
        "汤姆 sees the 记事本 at the 床头柜."
    ],
    
    # 时间1: 莉莉移动药瓶
    [
        "莉莉 moves the 药瓶 to the 卧室抽屉.",
        "梅姨 did not see the 药瓶 move to the 卧室抽屉.",  # 随机决定
        "汤姆 saw the 药瓶 move to the 卧室抽屉."           # 随机决定
    ],
    
    # 时间2: 梅姨移动记事本
    [...],
    
    # 时间3: 汤姆移动药瓶
    [...]
]
```

---

### 2. `beliefs` - 信念状态（最关键！）

**结构**: `beliefs[时间点][人名][物品名] = 该人认为物品在的位置`

```python
beliefs = [
    # 时间0: 初始 - 所有人都知道真实位置
    {
        "梅姨": {"药瓶": "镜子后面", "记事本": "床头柜"},
        "莉莉": {"药瓶": "镜子后面", "记事本": "床头柜"},
        "汤姆": {"药瓶": "镜子后面", "记事本": "床头柜"}
    },
    
    # 时间1: 莉莉把药瓶移到卧室抽屉（假设梅姨没看到，汤姆看到了）
    {
        "梅姨": {"药瓶": "镜子后面", "记事本": "床头柜"},  # ⚠️ 梅姨还以为在镜子后面！
        "莉莉": {"药瓶": "卧室抽屉", "记事本": "床头柜"},  # 莉莉自己移的，知道新位置
        "汤姆": {"药瓶": "卧室抽屉", "记事本": "床头柜"}   # 汤姆看到了
    },
    
    # 时间2: 梅姨把记事本移到客厅茶几（假设莉莉没看到，汤姆看到了）
    {
        "梅姨": {"药瓶": "镜子后面", "记事本": "客厅茶几"},  # 梅姨自己移的记事本
        "莉莉": {"药瓶": "卧室抽屉", "记事本": "床头柜"},    # ⚠️ 莉莉不知道记事本移了！
        "汤姆": {"药瓶": "卧室抽屉", "记事本": "客厅茶几"}
    },
    
    # 时间3: 汤姆把药瓶移到浴室药箱（假设梅姨看到了，莉莉没看到）
    {
        "梅姨": {"药瓶": "浴室药箱", "记事本": "客厅茶几"},  # 梅姨看到了汤姆移药瓶
        "莉莉": {"药瓶": "卧室抽屉", "记事本": "床头柜"},    # ⚠️ 莉莉完全不知道后续变化！
        "汤姆": {"药瓶": "浴室药箱", "记事本": "客厅茶几"}   # 汤姆自己移的
    }
]
```

**这就是为什么可以出题**：`beliefs[-1]` 是最终信念状态，每个人对物品位置的认知可能不同！

---

### 3. `actual_locs` - 实际位置

```python
actual_locs = [
    # 时间0
    {"药瓶": "镜子后面", "记事本": "床头柜"},
    
    # 时间1: 药瓶被移动
    {"药瓶": "卧室抽屉", "记事本": "床头柜"},
    
    # 时间2: 记事本被移动
    {"药瓶": "卧室抽屉", "记事本": "客厅茶几"},
    
    # 时间3: 药瓶再次被移动
    {"药瓶": "浴室药箱", "记事本": "客厅茶几"}
]
```

---

### 4. `event_structure` - 结构化事件（用于构建逻辑树）

```python
event_structure = [
    # 开场
    {
        "event": "opening scene",
        "immutable_sequence": [
            "梅姨 sees the 药瓶 at the 镜子后面.",
            "莉莉 sees the 药瓶 at the 镜子后面.",
            "汤姆 sees the 药瓶 at the 镜子后面.",
            "梅姨 sees the 记事本 at the 床头柜.",
            "莉莉 sees the 记事本 at the 床头柜.",
            "汤姆 sees the 记事本 at the 床头柜."
        ],
        "sequence": []  # 开场没有需要推理的观察
    },
    
    # 移动1
    {
        "event": "莉莉 moves the 药瓶 to the 卧室抽屉.",
        "immutable_sequence": [],  # 移动时在目标位置看到的其他物品
        "sequence": [
            "梅姨 did not see the 药瓶 move to the 卧室抽屉.",  # 需要解释为什么
            "汤姆 saw the 药瓶 move to the 卧室抽屉."            # 需要解释为什么
        ]
    },
    
    # 移动2
    {
        "event": "梅姨 moves the 记事本 to the 客厅茶几.",
        "immutable_sequence": [],
        "sequence": [
            "莉莉 did not see the 记事本 move to the 客厅茶几.",
            "汤姆 saw the 记事本 move to the 客厅茶几."
        ]
    },
    
    # 移动3
    {
        "event": "汤姆 moves the 药瓶 to the 浴室药箱.",
        "immutable_sequence": [],
        "sequence": [
            "梅姨 saw the 药瓶 move to the 浴室药箱.",
            "莉莉 did not see the 药瓶 move to the 浴室药箱."
        ]
    }
]
```

---

## 信念状态更新逻辑图

```
                    时间0 (初始)              时间1 (莉莉移动药瓶)         时间2 (梅姨移动记事本)        时间3 (汤姆移动药瓶)
                    ═══════════              ═══════════════════        ═══════════════════════      ═══════════════════════
实际位置:
  药瓶              镜子后面       ───────►    卧室抽屉         ───────►    卧室抽屉         ───────►    浴室药箱
  记事本            床头柜        ───────►    床头柜          ───────►    客厅茶几         ───────►    客厅茶几


信念状态:
  ┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │ 梅姨                                                                                                             │
  │   药瓶:  镜子后面    ─(没看到)──►  镜子后面 ❌    ─(没变化)──►  镜子后面 ❌   ─(看到了)──►  浴室药箱 ✓        │
  │   记事本: 床头柜     ─(没变化)──►  床头柜 ✓     ─(自己移)──►  客厅茶几 ✓    ─(没变化)──►  客厅茶几 ✓        │
  └─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
  ┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │ 莉莉                                                                                                             │
  │   药瓶:  镜子后面    ─(自己移)──►  卧室抽屉 ✓    ─(没变化)──►  卧室抽屉 ❌   ─(没看到)──►  卧室抽屉 ❌        │
  │   记事本: 床头柜     ─(没变化)──►  床头柜 ✓     ─(没看到)──►  床头柜 ❌    ─(没变化)──►  床头柜 ❌          │
  └─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘
  ┌─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
  │ 汤姆                                                                                                             │
  │   药瓶:  镜子后面    ─(看到了)──►  卧室抽屉 ✓    ─(没变化)──►  卧室抽屉 ✓    ─(自己移)──►  浴室药箱 ✓        │
  │   记事本: 床头柜     ─(没变化)──►  床头柜 ✓     ─(看到了)──►  客厅茶几 ✓    ─(没变化)──►  客厅茶几 ✓        │
  └─────────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

✓ = 信念与实际一致    ❌ = 信念与实际不一致（可以出题！）
```

---

## 核心更新规则

```python
for subject in [非移动者]:
    sees_update = random.random() < chance_subject_sees  # 33%概率看到
    
    if sees_update:
        # 看到了 → 更新信念为新位置
        beliefs[subject][item] = new_location
    else:
        # 没看到 → 保持旧信念！
        beliefs[subject][item] = beliefs[-1][subject][item]  # 保持上一时刻的信念
```

---

## 问题生成的依据

根据 `beliefs[-1]`（最终信念状态）：

| 人 | 物品 | 信念位置 | 实际位置 | 可出题? |
|---|-----|---------|---------|--------|
| 梅姨 | 药瓶 | 浴室药箱 | 浴室药箱 | ✓ (但她是看到的) |
| 梅姨 | 记事本 | 客厅茶几 | 客厅茶几 | ❌ (自己移的，排除) |
| 莉莉 | 药瓶 | 卧室抽屉 | 浴室药箱 | ✓ **最佳问题** |
| 莉莉 | 记事本 | 床头柜 | 客厅茶几 | ✓ **最佳问题** |
| 汤姆 | 药瓶 | 浴室药箱 | 浴室药箱 | ❌ (自己移的，排除) |
| 汤姆 | 记事本 | 客厅茶几 | 客厅茶几 | ✓ |

**生成的问题示例**：
- "莉莉最可能去哪里找药瓶？" → 答案: **卧室抽屉**（她以为的位置，不是实际位置）
- "莉莉最可能去哪里找记事本？" → 答案: **床头柜**